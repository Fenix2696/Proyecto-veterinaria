{
  "name": "veterinaria-proyecto",
  "version": "1.0.0",
  "description": "Proyecto completo de veterinaria",
  "scripts": {
    "start": "cd backend && npm install && node server.js",
    "build": "npm install && cd backend && npm install && cd ../frontend && npm install && npm run build",
    "postinstall": "cd backend && npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^4.1.0"
  }
}
